// String
const name = 'Max';
// Number
const number = 500;

// Null
const houseSomeday = null;

// Undefined
const walking = undefined;

// Boolean
const lazy = true;

// Array
const shoppingList = [name, number, houseSomeday, walking, lazy];
console.log(person);
console.log(shoppingList);
// Object
const person = {
    name: name,
    pay: number,
    house: houseSomeday,
    excersise: walking,
    lazy: lazy
};

console.log(person);